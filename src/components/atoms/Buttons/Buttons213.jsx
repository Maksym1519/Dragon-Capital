import b from '../button.module.scss';

const Button213 = (props) => {
    return (
        <div className={b.button213__wrapper}>
           {props.text}
        </div>
    )
}

export default Button213;
