import b from '../button.module.scss';

const Button233 = (props) => {
    return (
        <div className={b.button233__wrapper}>
           {props.text}
        </div>
    )
}

export default Button233;
