import t from './text.module.scss';

const Text14700 = (props) => {
    return (
        <p className={t.text14700} style={props.color}>
            {props.text}
        </p>
    )
}

export default Text14700;